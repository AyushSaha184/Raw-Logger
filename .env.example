# Required for live Gemini calls. Tests and --no-llm do not need this.
GEMINI_API_KEY=your_key_here

# Optional model overrides.
LOG_CORR_PRO_MODEL=gemini-2.5-pro
LOG_CORR_FLASH_MODEL=gemini-2.5-flash

# Disable external LLM calls.
LOG_CORR_NO_LLM=false
